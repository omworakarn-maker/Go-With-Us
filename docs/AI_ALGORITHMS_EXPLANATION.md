# เจาะลึกอัลกอริทึมและตัวอย่างการคำนวณแบบ Step-by-Step (โปรเจกต์ Go With Us)

เอกสารนี้จัดทำขึ้นเพื่อใช้อธิบายกระบวนการทำงานเบื้องหลังระบบ AI ทั้ง 3 ตัวในโปรเจกต์ **Go With Us** เผื่อใช้สำหรับตอบคำถามอาจารย์ ทำสไลด์ หรือเขียนลงในรายงานบทที่ 3 เพื่อให้เห็นภาพรวมและ "วิชาคณิตศาสตร์ (The Math)" ที่ซ่อนอยู่ใต้ระบบอย่างละเอียดและเข้าใจง่ายครับ

---

## 🧠 ระบบที่ 1: การจัดกลุ่มผู้ใช้งาน (Advanced User Clustering ด้วย K-Means)

**เป้าหมาย:** ทันทีที่ผู้ใช้ใหม่สมัครและทำแบบสอบถามเสร็จ ระบบจะนำข้อมูลมาจัดเข้า "กลุ่มลักษณะนิสัยการท่องเที่ยว (Micro-segmentation)" เพื่อใช้เป็นฐานข้อมูลตั้งต้นในการแนะนำเพื่อนและทริป

**💡 ทฤษฎีและสมการ:**
อัลกอริทึม K-Means ใช้สมการการหาระยะห่างที่เรียกว่า **Euclidean Distance** เพื่อคำนวณหาว่า "ผู้ใช้คนนี้ มีจุดคะแนนนิสัยใกล้เคียงกับจุดศูนย์กลางของกลุ่มไหนมากที่สุด?" หากคะแนนใกล้กลุ่มใดสุด ก็จะถูกจัดให้อยู่ในกลุ่มนั้น

> **สมการหาระยะห่าง:** `d = √((x₂ - x₁)² + (y₂ - y₁)²) ...`
> *(เมื่อ x, y คือคะแนนประเมินแต่ละด้าน เช่น ด้านงบประมาณ ด้านความลุย)*

**📝 ตัวอย่างการจำลองคำนวณ:**
สมมติว่าในระบบเราตัดมาคิดแค่พฤติกรรมหลัก 2 แกน (คะแนนเต็ม 10)
- **แกน X = ระดับกำลังทรัพย์ (Budget):** 1 = ประหยัดสุด, 10 = หรูหราสุด
- **แกน Y = ความชอบผจญภัย (Adventure):** 1 = นอนชิลคาเฟ่, 10 = ลุยป่าปีนเขา

ระบบหลังบ้านได้ตั้ง **จุดกึ่งกลาง (Centroid)** ของกลุ่มนักท่องเที่ยวไว้ 2 กลุ่มเบื้องต้น:
- **กลุ่ม 1 (สายชิลประหยัด):** ตำแหน่ง Centroid_1 มีค่าแกน = `(X: 3, Y: 3)`
- **กลุ่ม 2 (สายลุยทรงซ้อ/หรูหรา):** ตำแหน่ง Centroid_2 มีค่าแกน = `(X: 8, Y: 8)`

**สถานการณ์:**
นาย A เพิ่งโหลดแอปมา ทำแบบสอบถามเสร็จ ผลประเมินออกมาว่า:
- **นาย A:** ชอบเที่ยวแบบชิลๆ ไม่ลุยเด็ดขาด แถมขอทริปที่ประหยัดมากๆ = `(X: 1, Y: 2)`

**AI เริ่มคำนวณเพื่อตัดนาย A เข้ากลุ่ม:**
1. หาระยะห่างนาย A เทียบกับกลุ่ม 1: `d₁ = √((1 - 3)² + (2 - 3)²) = √((-2)² + (-1)²) = √(4 + 1) = √5 ≈ 2.23`
2. หาระยะห่างนาย A เทียบกับกลุ่ม 2: `d₂ = √((1 - 8)² + (2 - 8)²) = √((-7)² + (-6)²) = √(49 + 36) = √(85) ≈ 9.21`

**✅ ผลลัพธ์ตัดสิน:** เมื่อนำ `d₁` และ `d₂` มาเทียบกัน ระยะห่างระหว่างนาย A กับกลุ่ม 1 (2.23) สั้นกว่ากลุ่ม 2 (9.21) อย่างชัดเจน อัลกอริทึมจึงจับนาย A โยนเข้าสู่ **"กลุ่ม 1 (สายชิลประหยัด)"** โดยอัตโนมัติ

---

## 📈 ระบบที่ 2: พยากรณ์ความสำเร็จของทริป (Predictive Matching ด้วย Random Forest)

**เป้าหมาย:** ทำนายและแสดงผลเป็น "เปอร์เซ็นต์ความเข้ากันได้ (% Success Rate)" เพื่อประกอบการตัดสินใจ เมื่อนาย A กำลังเล็งว่าจะกดขอร่วมทริปของนาย B ดีหรือไม่

**💡 ทฤษฎีและสมการ:**
Random Forest ทำงานโดยการสร้าง "ต้นไม้ตัดสินใจ (Decision Trees)" จำนวนมหาศาล เพื่อสุ่มตรวจความเข้ากันได้จากหลายๆ มุมมอง (อายุ, เพศ, สไตล์การนอน, กลุ่ม Cluster) จากนั้นนำผลลัพธ์ของต้นไม้ทุกต้นมา "หาค่าเฉลี่ยโหวต (Ensemble/Aggregation)" เพื่อลดความลำเอียง (Bias)
ในแต่ละโหนดของต้นไม้ จะใช้สูตรคำนวณความบริสุทธิ์ของข้อมูลในการแยกแยะเงื่อนไข เรียกว่า **Gini Impurity**

> **สมการ Gini Impurity:** `Gini = 1 - Σ (P_i)²`

**📝 ตัวอย่างการจำลองวิธีคิดเบื้องหลัง:**
**สถานการณ์:** นาย A (อายุ 25, นอน Hostel ร่วมได้, ตื่นสาย) อยากร่วมทริปที่จัดโดย นาย B (อายุ 30, ชอบนอน Hotel ส่วนตัว, ตื่นเช้า)
AI จ่ายข้อมูลของ 2 คนนี้ผ่านเข้าไปในป่า (สมมติว่าแบบจำลองนี้ดึงต้นไม้ออกมาอธิบาย 3 ต้น):

- **Tree 1 (มุมมอง: อายุ + เวลาตื่น):**
  - *กฎจากข้อมูลสถิติในอดีต:* ถ้าอายุห่างกันไม่เกิน 5 ปี (ผ่าน/ให้คะแนนบวก) แต่ไลฟ์สไตล์ตื่นนอนต่างกันสุดขั้ว (ตก/ให้คะแนนลบหนัก)
  - ผลประเมินคะแนนความเข้ากันได้จากต้นไม้นี้ = **40%**
- **Tree 2 (มุมมอง: ที่พัก + เวลาตื่น):**
  - *กฎจากข้อมูลสถิติในอดีต:* สไตล์การรุมนอนต่างกัน (Hostel vs Hotel) (ตก) แถมตื่นไม่พร้อมกันอีก (ตก) เสี่ยงต่อการทะเลาะกันมาก
  - ผลประเมินคะแนนความเข้ากันได้จากต้นไม้นี้ = **10%**
- **Tree 3 (มุมมอง: กลุ่ม Cluster + ความสนใจสถานที่):**
  - *กฎจากข้อมูลสถิติในอดีต:* ทั้งคู่ชอบไปเที่ยวมิวเซียมเหมือนกัน (ผ่าน) และจัดอยู่ใน Cluster 1 (สายชิลประหยัด) เหมือนกัน (ผ่าน)
  - ผลประเมินคะแนนความเข้ากันได้จากต้นไม้นี้ = **85%**

**✅ ผลลัพธ์ตัดสินตัวเลขหน้าแอป (Aggregation):**
`Success Rate (โอกาสสำเร็จของ A และ B) = หาค่าเฉลี่ยของทุกต้นไม้ = (40 + 10 + 85) / 3 = 45%`
*(หน้า UI ของแอปจะแจ้งเตือนนาย A ว่า: "คุณมีโอกาสเข้ากับโฮสต์ทริปนี้ได้เพียง 45% เนื่องจากธรรมชาติการพักผ่อนและการตื่นนอนไม่ตรงกัน อาจเกิดความอึดอัดได้")*

---

## ⚙️ ระบบที่ 3: ปรับจูนนิสัยจากรีวิวอัตโนมัติ (Review Sentiment to Travel DNA)

**เป้าหมาย:** แก้ปัญหาพฤติกรรม "โปรไฟล์ไม่ตรงปก" โดยการใช้ AI อ่านข้อความคอมเมนต์ทริป (Text Review) เพื่อนำมา "ปรับลดพอยต์/เพิ่มพอยต์" ในแกนนิสัยต่างๆ (Travel DNA) อัตโนมัติ โดยที่คนถูกรีวิวไม่รู้ตัว

**💡 ทฤษฎีและสมการ:**
1. **NLP Processing (เช่น BERT หรือ Gemini API):** สกัดความรู้สึกจากข้อความให้ออกมาเป็นตัวเลข (Sentiment Score: สเกล -1 ถึง 1 หรือแปลงคณิตศาสตร์กลับเป็น 1-10)
2. **Exponential Moving Average (EMA):** ใช้สมการอัปเดตแบบถ่วงน้ำหนัก เพื่อป้องกันปัญหาเจอรีวิวกลั่นแกล้ง 1 ครั้งแล้วคะแนนพังทลาย (ระบบจะให้น้ำหนักประวัติการทำดีเก่าๆ ด้วย)

> **สมการหาทราเวล DNA ใหม่:** `New_DNA = (Old_DNA × α) + (New_Sentiment_Score × (1 - α))`
> *(เมื่อ α (Alpha) คือค่าน้ำหนักของความเชื่อถือในประวัติเก่า เช่น ให้ประวัติเก่ามีผล 70% และผลรีวิวรอบใหม่ล่าสุดมีผลแค่ 30%)*

**📝 ตัวอย่างการจำลองคำนวณ:**
สมมติคะแนน DNA ในหมวด **"ความตรงต่อเวลา (Punctuality)"** ของนาย B มีค่าอยู่ที่ **9 / 10** คะแนน (ถือว่าดีมาก)

- **Step 1: NLP สกัดข้อความ (Extraction)**
  พอกลับจากทริปล่าสุด นาย A พิมพ์รีวิวนาย B ในแอปว่า: *"พี่คนนี้นิสัยดีครับคุยสนุก แต่แกตื่นสายกว่าเวลานัดไปเกือบ 2 ชั่วโมง ทริปเลยเลทเวลาเผื่อไว้หมดเลยแอบเซ็งนิดนึง"*
  AI อ่านเจอ Keyword: "ตื่นสาย", "เลท", "เซ็ง" จึงให้คะแนนความน่าจะเป็นในหมวดตรงเวลา (Punctuality) เป็นแง่ลบ
  สมมติ AI ดรอป Sentiment Score ของทริปครั้งนี้กลับมาให้ = **2 / 10** คะแนน

- **Step 2: อัปเดตนิสัย (Calculate New DNA)**
  แทนค่าน้ำหนัก (α) = 0.7 (ให้ความเชื่อมั่นของเก่า 70%)
  - `New_DNA = (คะแนนเก่า 9 × 0.7) + (คะแนนรอบนี้จากข้อความ 2 × 0.3)`
  - `New_DNA = (6.3) + (0.6)`
  - `New_DNA = 6.9`

**✅ ผลลัพธ์ตัดสิน:**
ระบบเบื้องหลังจะทำการแอบอัปเดต "ค่าความตรงต่อเวลา" ในโปรไฟล์ (Travel DNA) ของนาย B จาก 9.0 ร่วงลงมาเหลือ **6.9** โดยอัตโนมัติ
**และลูปสุดล้ำก็คือ:** พอนาย B จะไปกดขอร่วมแจมในทริปต่อไป... ข้อมูล DNA = 6.9 นี้แหละ จะถูกโยนกลับไปในระบบอัลกอริทึม **ข้อ 2 (Random Forest)** ทำให้อนาคต ถ้านาย B พยายามไปแจมกับคนสแกนเรื่องเวลาเป๊ะๆ โอกาสความเข้ากันได้ (Success Rate) จะถูกคำนวณลดลงอย่างสมเหตุสมผล!

---

## 🎤 ทริค: วิธีตอบคำถามอาจารย์กรรมการสอบ

**หากอาจารย์ถาม:** *"อ้าว ทำไมเราถึงต้องไปตั้งค่าใช้โมเดล AI ยุ่งยากพวกนี้ด้วย? ทำไมเราไม่เขียนโค้ดแค่เช็ค IF/ELSE ธรรมดาในฐานข้อมูล เช่น IF คนนี้ตื่นเช้า == คนตื่นเช้า ถึงให้ผ่าน แบบนี้ไม่ได้หรอ?"*

**เทคนิคการตอบ (Answer Key):**
> "ถ้าเราเปรียบเทียบแค่ IF/ELSE หรือข้อมูลโปรไฟล์ปกติ มันจะไม่สะท้อน **'ธรรมชาติกึ่งซับซ้อนของมนุษย์'** ครับอาจารย์ 
> มนุษย์มีความยืดหยุ่น ถ้าเราใช้กฎ IF/ELSE แข็งๆ คนที่ตื่นเช้ากับคนที่ตื่นสาย ระบบจะปัดตกทันที 0% แต่ในความเป็นจริง Machine Learning อย่าง Random Forest สามารถค้นพบ Pattern ที่ซ่อนอยู่ได้ เช่น จากสถิติพบว่า แม้จะมีคนตื่นสายแต่ถ้าพวกเขาไปจัดทริปสายคาเฟ่ถ่ายรูป เขากลับเข้ากันได้ดีและทริปสำเร็จสูงกว่าปกติ เป็นต้น
> 
> นอกจากนี้ โมเดลอย่างระบบวิเคราะห์รีวิว (NLP) ที่เชื่อมด้วยสมการ EMA (เพื่อให้คะแนน DNA ค่อยๆ ขยับตามความเป็นจริง) จะช่วยให้เราสร้าง **Data Feedback Loop** หมายความว่า ยิ่งเพื่อนร่วมทริปรีวิวกันมากเท่าไหร่ AI ก็จะคำนวณปรับจุดศูนย์กลางของผู้ใช้ (Centroids) และค่า DNA ให้เข้าใกล้ความเป็นจริงของคนคนนั้นมากขึ้นตลอดเวลา ทำให้ระบบและแอปพลิเคชันของเรา **'ยิ่งมีคนใช้เยอะ ยิ่งฉลาด และยิ่งทำนายเพื่อนร่วมทริปได้แม่นยำขึ้นเรื่อยๆ'** ซึ่งไม่สามารถทำได้ด้วยการ Query ฐานข้อมูลแบบ SQL ธรรมดาครับ"
